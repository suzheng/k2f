# Guide images

This directory holds PNG screenshots for the user guide (`docs/guide/`).

Use official **2×** renders from the lock executor (for example `k2f render examples/published/invoice.K2F --page 0 -o docs/guide/images/invoice-page-1.png`).

`.gitignore` whitelists `docs/guide/images/*.png` for committed documentation assets.
